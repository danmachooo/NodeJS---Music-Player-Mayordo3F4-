<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Audio List</title>
</head>
<body>
    <h1>Audios</h1>
    <a href="/upload">Upload New Audio</a>
    <ul>
        <% audios.forEach(audio => { %>
            <li>
                <a href="/play/<%= audio.id %>"><%= audio.title %></a>
            </li>
        <% }) %>
    </ul>
</body>
</html>
